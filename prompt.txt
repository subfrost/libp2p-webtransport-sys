### Prompt for Updating `libp2p-webtransport-sys`

**Goal:**

Modify the `libp2p-webtransport-sys` crate to correctly handle TLS certificate validation using the `/certhash/` multiaddress protocol. The client should bypass standard PKI trust chain validation and rely exclusively on the certificate hash when one is provided in the dialing address.

**Problem Description:**

Currently, when a client attempts to dial a WebTransport address that uses a certificate from a non-standard Certificate Authority (like a Let's Encrypt certificate without the system having the correct root CAs), the connection fails with a `rustls::Error::InvalidCertificate(CertificateError::UnknownIssuer)`. This failure occurs even when a valid `/certhash/` is provided in the multiaddress. The TLS handshake fails before the certificate hash can be verified, rendering the `certhash` mechanism ineffective for this use case.

**Proposed Solution:**

Implement a custom `rustls::client::danger::ServerCertVerifier`. This verifier will be dynamically applied to the `wtransport` client configuration only when dialing an address containing one or more `/certhash/` components.

The custom verifier will:
1.  Ignore the certificate's issuer and trust chain.
2.  Calculate the SHA-256 hash of the server's presented certificate (`end_entity`).
3.  Verify that this calculated hash matches one of the hashes provided in the `/certhash/` multiaddress components.
4.  If a match is found, the certificate is considered valid. Otherwise, the connection is rejected.

**Implementation Details:**

**1. Create the Custom Verifier (`src/transport/mod.rs`):**

A struct `WebTransportServerVerifier` that implements `ServerCertVerifier` is already present. It needs to be enhanced with logging to ensure it's working correctly.

```rust
// In reference/libp2p-webtransport-sys/src/transport/mod.rs

use log;
use rustls::client::danger::{ServerCertVerified, ServerCertVerifier};
use sha2::{Digest, Sha256};

#[derive(Debug)]
struct WebTransportServerVerifier {
    hashes: Vec<wtransport::tls::Sha256Digest>,
}

impl WebTransportServerVerifier {
    fn new(hashes: Vec<wtransport::tls::Sha256Digest>) -> Self {
        Self { hashes }
    }
}

impl ServerCertVerifier for WebTransportServerVerifier {
    fn verify_server_cert(
        &self,
        end_entity: &rustls::pki_types::CertificateDer<'_>,
        _intermediates: &[rustls::pki_types::CertificateDer<'_>],
        _server_name: &rustls::pki_types::ServerName<'_>,
        _ocsp_response: &[u8],
        _now: rustls::pki_types::UnixTime,
    ) -> Result<ServerCertVerified, rustls::Error> {
        let cert_hash = Sha256::digest(end_entity.as_ref());
        let cert_hash_w = wtransport::tls::Sha256Digest::new(cert_hash.into());

        log::debug!("Verifier: received cert with hash: {:?}", cert_hash);
        log::debug!("Verifier: expected one of hashes: {:?}", self.hashes);

        if self.hashes.contains(&cert_hash_w) {
            log::debug!("Verifier: SUCCESS, certificate hash matches");
            Ok(ServerCertVerified::assertion())
        } else {
            log::error!("Verifier: FAILED, certificate hash does not match");
            Err(rustls::Error::InvalidCertificate(
                rustls::CertificateError::UnknownIssuer,
            ))
        }
    }

    // Other trait methods can return default/Ok values
    fn verify_tls12_signature(
        &self,
        _message: &[u8],
        _cert: &rustls::pki_types::CertificateDer<'_>,
        _dss: &rustls::DigitallySignedStruct,
    ) -> Result<rustls::client::danger::HandshakeSignatureValid, rustls::Error> {
        Ok(rustls::client::danger::HandshakeSignatureValid::assertion())
    }

    fn verify_tls13_signature(
        &self,
        _message: &[u8],
        _cert: &rustls::pki_types::CertificateDer<'_>,
        _dss: &rustls::DigitallySignedStruct,
    ) -> Result<rustls::client::danger::HandshakeSignatureValid, rustls::Error> {
        Ok(rustls::client::danger::HandshakeSignatureValid::assertion())
    }

    fn supported_verify_schemes(&self) -> Vec<rustls::SignatureScheme> {
        vec![
            rustls::SignatureScheme::RSA_PKCS1_SHA256,
            rustls::SignatureScheme::ECDSA_NISTP256_SHA256,
            // Add other schemes as needed
        ]
    }
}
```

**2. Update the Dial Logic (`src/transport/mod.rs`):**

Modify the `dial` method in the `impl libp2p::Transport for WebTransport` block to conditionally create and use the custom verifier.

```rust
// In reference/libp2p-webtransport-sys/src/transport/mod.rs inside the dial method

// ... inside the `Ok(Box::pin(async move { ...` block
let hashes = s_addr
    .certhashes
    .clone()
    .into_iter()
    .map(|mh| {
        mh.digest()
            .try_into()
            .map(wtransport::tls::Sha256Digest::new)
            .map_err(|_| Error::InvalidCerthash)
    })
    .collect::<Result<Vec<_>, _>>()?;

let client_config = if !hashes.is_empty() {
    // If certhashes are present, use the custom verifier
    log::debug!("dialing with certhashes: {:?}", hashes);
    let mut root_store = rustls::RootCertStore::empty(); // IMPORTANT: Do not use native roots
    let mut client_config = rustls::ClientConfig::builder()
        .with_root_certificates(root_store)
        .with_no_client_auth();
    
    client_config
        .dangerous()
        .set_certificate_verifier(std::sync::Arc::new(WebTransportServerVerifier::new(hashes)));
    
    client_config.alpn_protocols = vec![b"h3".to_vec()];
    
    wtransport::ClientConfig::builder()
        .with_bind_default()
        .with_custom_tls(client_config)
        .build()
} else {
    // Otherwise, use default TLS validation
    log::debug!("dialing without certhashes, using default TLS validation");
    wtransport::ClientConfig::builder()
        .with_bind_default()
        .with_native_certs()
        .build()
};

let endpoint = wtransport::Endpoint::client(client_config).unwrap();
// ... rest of the dial logic
```

**Testing Requirements:**

Create a new test in `tests/transport.rs` to validate this functionality.

1.  **Test Name:** `dial_and_listen_with_certhash_and_acme_cert`
2.  **Setup:**
    *   The test should be `#[ignore]`d by default as it requires a specific local setup.
    *   It must load the `fullchain.cer` and `filler-season-apathy.sandshrew.io.pkcs8.key` from the `~/.acme.sh/filler-season-apathy.sandshrew.io_ecc/` directory. Use the `shellexpand` crate.
3.  **Listener:**
    *   Create a `WebTransport` listener configured with `Config::CertificateFromMemory` using the loaded ACME certificate and key.
4.  **Certhash Calculation:**
    *   Parse the `fullchain.cer` file using the `pem` crate.
    *   Calculate the SHA-256 hash of the *first* certificate in the PEM file. This is the end-entity certificate.
    *   Create a `Multihash` from this digest.
5.  **Dialer:**
    *   Create a `WebTransport` dialer.
    *   Construct a `Multiaddr` that includes:
        *   The DNS address: `/dns4/filler-season-apathy.sandshrew.io/...`
        *   The correct `/certhash/...` protocol with the calculated multihash.
        *   The listener's PeerId.
    *   Dial this multiaddress.
6.  **Assertions:**
    *   The dial operation must succeed.
    *   The connection must be established.
    *   (Optional but recommended) Add a negative test case where the dialer uses an *incorrect* `certhash` and assert that the connection fails.

**Final Steps:**

1.  Commit and push the changes to the `subfrost` branch of the `libp2p-webtransport-sys` repository.
2.  In the `subfrost` project, run `cargo update -p libp2p-webtransport-sys` to fetch the updated dependency.
3.  Run the main test in `crates/subp2p/tests/relay_test.rs` to confirm the fix.